{
  "name": "discord-loa-bot",
  "version": "2.0.0",
  "description": "Discord LOA bot with role whitelist – production-ready",
  "main": "dist/index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "deploy": "tsx src/deploy-commands.ts",
    "prestart": "npm run build"
  },
  "dependencies": {
    "discord.js": "^14.14.1",
    "mongoose": "^8.1.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "rimraf": "^5.0.5",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}